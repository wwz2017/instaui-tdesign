{
  "name": "npm-src",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build only insta": "vite build && uv run --no-project scripts/builder.py",
    "build": "vite build && vite build --config vite.config.tdesign.ts && uv run --no-project scripts/builder.py",
    "build:tdesign": "vite build --config vite.config.tdesign.ts",
    "build only": "vite build"
  },
  "dependencies": {
    "tdesign-vue-next": "^1.15.5",
    "vue": "^3.5.17"
  },
  "devDependencies": {
    "@types/node": "^24.0.10",
    "@vitejs/plugin-vue": "^6.0.0",
    "@vue/tsconfig": "^0.7.0",
    "typescript": "~5.8.3",
    "vite": "^7.0.0",
    "vitest": "^3.2.4",
    "vue-tsc": "^2.2.10"
  }
}